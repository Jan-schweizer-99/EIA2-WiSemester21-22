var random_test;
(function (random_test) {
    //let list: string[] = ["klaus", "peter", "Dieta", "HÄRBERT"];
    //list = list.sort(() => Math.random() - 0.5);
    //console.log(list);
    //  window.addEventListener("load", handleLoad); //starte die handleload funktion
    //  function handleLoad(_event: Event): void {
    //window.prompt.apply(Load);
    var codeword = prompt("Please enter your name", "EIA-2-stinkt");
    var chars = codeword.split("");
    console.log(chars);
    var counter = 0;
    //  }
    function Load() {
        console.log("hat geklapt");
    }
})(random_test || (random_test = {}));
//# sourceMappingURL=memory.js.map